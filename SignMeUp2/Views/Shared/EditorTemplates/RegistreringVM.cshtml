@using SignMeUp2.ViewModels
@using SignMeUp2.Data

@model RegistreringVM

@Html.HiddenFor(model => model.FormularId)
@Html.HiddenFor(model => model.AttBetala)
@Html.HiddenFor(model => model.Forseningsavgift)
@Html.HiddenFor(model => model.HarBetalt)
@Html.HiddenFor(model => model.PaysonToken)
@Html.HiddenFor(model => model.Rabattkod)
@Html.HiddenFor(model => model.Registreringstid)

<table class="table table-striped">
    <thead>
        <tr>
            <th colspan="2"><h3>Anmälan</h3></th>
        </tr>
    </thead>

    @foreach (FormularStegVM steg in Model.Formular.Steg)
    {
        <tr>
            <th colspan="2"><h4>@steg.Namn</h4></th>
        </tr>

        foreach (var falt in steg.FaltLista)
        {
            var svar = Model.Svar.Single(s => s.FaltId == falt.FaltId);

            // Om det ska vara en drop-down
            if (falt.Typ == FaltTyp.val_falt)
            {
                <tr>
                    <th>
                        @Html.LabelFor(model => svar.Varde, falt.Namn, htmlAttributes: new { @class = "control-label col-md-1" })
                    </th>
                    <td class="col-md-11">
                        @Html.DropDownListFor(model => svar.Varde, falt.Alternativ, "Välj " + falt.Namn, htmlAttributes: new { @class = "form-control", @Name = falt.Namn })
                    </td>
                </tr>
            }
            // Om det är ett textfält
            else if (falt.Typ == FaltTyp.text_falt || falt.Typ == FaltTyp.epost_falt)
            {
                <tr>
                    <th>
                        @Html.LabelFor(model => svar.Varde, falt.Namn, htmlAttributes: new { @class = "control-label col-md-1" })
                    </th>
                    <td>
                        @Html.EditorFor(model => svar.Varde, new { htmlAttributes = new { @autofocus = "autofocus", @class = "form-control", @Name = falt.Namn } })
                    </td>
                </tr>
            }

            @*var text = "";

            // Om det är ett fal-fält slår vi upp namnet på valet
            if (falt.Typ == FaltTyp.val_falt)
            {
                var id = int.Parse(svar.Varde);
                var val = falt.Val.FirstOrDefault(f => f.Id == id);
                text = val.Namn;
            }
            else
            {
                text = svar.Varde;
            }

            <tr>
                <th>@svar.Falt.Namn</th>
                <td>@text</td>
            </tr>
            }*@
        }
    }

    @if (Model.Invoice != null)
    {
        <tr>
            <th colspan="2"><h3>Fakturaadress</h3></th>
        </tr>
        <tr>
            <th>Länk</th>
            <td>@Html.ActionLink("Visa faktura för anmälan", "Faktura", "SignMeUp", new { id = model.Id }, null)</td>
        </tr>
        <tr>
            <th colspan="2"><h3>Fakturaadress</h3></th>
        </tr>
        <tr>
            <th>@Html.LabelFor(model => model.Namn)</th>
            <td>@Html.EditorFor(model => model.Namn)</td>
        </tr>
        <tr>
            <th>@Html.LabelFor(model => model.Organisationsnummer)</th>
            <td>@Html.EditorFor(model => model.Organisationsnummer)</td>
        </tr>
        <tr>
            <th>Attesteras</th>
            <td>@Html.EditorFor(model => model.Att)</td>
        </tr>
        <tr>
            <th>@Html.LabelFor(model => model.Postadress)</th>
            <td>@Html.EditorFor(model => model.Postadress)</td>
        </tr>
        <tr>
            <th>@Html.LabelFor(model => model.Postnummer)</th>
            <td>@Html.EditorFor(model => model.Postnummer)</td>
        </tr>
        <tr>
            <th>@Html.LabelFor(model => model.Postort)</th>
            <td>@Html.EditorFor(model => model.Postort)</td>
        </tr>
        <tr>
            <th>@Html.LabelFor(model => model.Box)</th>
            <td>@Html.EditorFor(model => model.Box)</td>
        </tr>
    }
</table>
