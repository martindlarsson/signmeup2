@if(Request.IsAuthenticated) {
    <text>Hej <strong>@User.Identity.Name</strong>!
    @*@Html.ActionLink("Log Off", "LogOff", "Account")*@
    [ @Html.ActionLink("Login", "Login", "Account", routeValues: new { Area = "" }, htmlAttributes: new { id = "loginLink" }) ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "Account") ]
}
